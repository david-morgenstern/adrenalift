# Adrenalift runtime + build dependencies.
#
# Lower bounds are the minimum versions known to work with the codebase.
# Upper bounds cap the next major release we have NOT yet validated against,
# so a future breaking release does not silently break the Windows build.
#
# Last green CI build (windows-latest, Python 3.12) resolved:
#   PySide6     6.11.1
#   pyinstaller 6.20.0
#   Flask       3.1.3
# See .github/workflows/build-windows.yml for the build matrix.

PySide6>=6.5.0,<7.0
pyinstaller>=6.0.0,<7.0
Flask>=3.0.0,<4.0
